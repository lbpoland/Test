# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/std/shadows/npc/goto_destination.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def stopped_route(self, ) -> None:
            if (!done) {
            mixed *rabbit
            rabbit = arg()
            if (objectp(rabbit[0])) {
            womble = rabbit[0]


    def move(self, ob: MudObject, in_thing: str, out_thing: str) -> int:
            int ret
            if (!done) {
            mixed *rabbit
            rabbit = arg()
            done = 1
            if (arrayp(rabbit) && objectp(rabbit[0])) {
            womble = rabbit[0]
            done = 2



class Goto_destination(MudObject):
