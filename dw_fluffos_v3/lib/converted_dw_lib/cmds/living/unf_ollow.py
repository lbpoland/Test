# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/cmds/living/unf_ollow.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def cmd(self, obs: MudObject) -> int:
            mapping hide_invis
            int hiding, sneaking
            object *ok, ob
            string s
            ok = [})
            foreach (ob in obs) {
            if (ob.remove_follower(TP)) {
            ok += [ob]



class Unf_ollow(MudObject):
