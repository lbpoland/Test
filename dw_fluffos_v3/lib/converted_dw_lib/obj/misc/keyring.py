# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/obj/misc/keyring.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def __init__(self):


    def long(self, word: str, dark: int) -> str:
            return ::long( word, dark ) +
            query_contents( "$C$"+ the_short() +" holds: " )


    def query_property(self, word: str) -> Any:
            object thing
            mixed arg
            arg = ::query_property( word )
            if ( arg ) {
            return arg


    def test_add(self, thing: MudObject, flag: int) -> int:
            if ( !thing.id( "key" ) && environment( thing ) ) {
            return 0


    def add_weight(self, n: int) -> int:
            int tmp, ret
            if(sizeof(all_inventory(self)) >= 15)
            return 0
            return ::add_weight(n)



class Keyring(Inventory):
