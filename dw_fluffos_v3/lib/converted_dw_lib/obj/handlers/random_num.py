# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/obj/handlers/random_num.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def set_seed(self, new_seed: int) -> None:
            int i
            if (seed <= 0)
            seed = efun::random(200)
            else
            seed = new_seed
            for (i=0;i<STARTUP_RANDS;i++)
            random(200)


    def query_seed(self, ) -> int:
            return seed



class Random_num(MudObject):
