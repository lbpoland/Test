# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/obj/handlers/bits_controller.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def __init__(self):
            self.bits = []


    def add_bit(self, ob: MudObject) -> None:
            if (!sizeof(bits))
            asyncio.create_task(self. (: do_update() :), TIME_OUT)
            bits += [ob]


    def remove_bit(self, ob: MudObject) -> None:
            int i
            if ((i = bits.index(ob) if ob in bits else -1) == -1)
            return
            bits = delete(bits, i, 1)


    def kickstart(self, ) -> None:
            do_update()



class Bits_controller(MudObject):

    bits = '[]'
