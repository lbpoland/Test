# Generated from /mnt/home2/test/Test/dw_fluffos_v3/lib/secure/simul_efun/debug.c (2003 FluffOS v3) for Forgotten Realms MUD
# Date: 2025-03-12 12:51:30
from typing import Dict, List, Optional, Any
from ..driver import MudObject, Player, driver
from ..driver import MudObject, Player, driver
import asyncio

    def debug_printf(self, fmt: str, args: Any) -> None:
            object env
            env = previous_object()
            if(env)
            while(environment(env))
            env = environment(env)
            if((!env || !env.query_is_room() || base_name(env) == "/room/rubbish") &&
            this_player()) {
            env = environment(this_player())



class Debug(MudObject):
